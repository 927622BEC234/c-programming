to find the biggest among two numbers:

#include <stdio.h>
int main(){
    int a,b;
    scanf("%d %d",&a,&b);
    if (a>b)
    {
printf("a is bigger number %d",a);
}
    else
    {
 printf("b is bigger number %d ",b);
  }
}
